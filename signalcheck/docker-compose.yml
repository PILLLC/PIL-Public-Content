version: '3.8'

services:
  signalcheck:
    build:
      context: ./app
    ports:
      - "5001:5001"
    volumes:
      - ./app:/app
    environment:
      - FLASK_ENV=development
    command: python app.py
    restart: unless-stopped
