version: '3.8'
services:
  ctfd:
    image: ctfd/ctfd
    container_name: ctfd
    ports:
      - "8000:8000"
    volumes:
      - ./ctfd-data:/var/ctfd
    environment:
      - CTFD_SECRET_KEY=mysecretkey
      - UPLOAD_FOLDER=/var/ctfd/uploads
    restart: always
